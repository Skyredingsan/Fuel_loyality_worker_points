meta:
  environment: golang
  toolchain: default

build:
  - go mod download
  - go build -o app cmd/api/main.go

run: ./app

ports:
  - 8080

env:
  - name: AMVERA
    value: "true"
  - name: DB_PATH
    value: /data/fuel-points.db
  - name: SERVER_PORT
    value: "8080"

persistenceMount: /data
